/** @type {import("prettier").Config} */
const config = {
  singleQuote: false,
  trailingComma: "all",
  tabWidth: 2,
  printWidth: 120,
  semi: true,
  quoteProps: "consistent",
};

export default config;
